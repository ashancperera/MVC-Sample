-  The folder should be extracted as the root of the domain
-  Create DB (news) and import news.sql 
-  Configure DB info on create_connection() in /protected/class/basemodel.php file

That is it!

